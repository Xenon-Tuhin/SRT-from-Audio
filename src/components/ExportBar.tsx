import {Stack,Button} from "@mui/material"; import type {DialogueSegment} from "../types/dialogue"; import {exportJSON,exportCSV,exportTXT,exportSRT} from "../utils/exporters"; export default function ExportBar({segments}:{segments:DialogueSegment[]}){return <Stack direction="row" spacing={1} flexWrap="wrap"><Button onClick={()=>exportJSON(segments)}>JSON</Button><Button onClick={()=>exportCSV(segments)}>CSV</Button><Button onClick={()=>exportTXT(segments)}>TXT</Button><Button variant="contained" onClick={()=>exportSRT(segments)}>SRT</Button></Stack>}